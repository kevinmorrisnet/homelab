apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv-homepage
spec:
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  nfs:
    server: 192.168.1.50
    path: /volume1/pvs/homepage
